# Slack Bot Configuration
# Copy this file to .env and fill in your actual tokens

# Bot User OAuth Token (starts with xoxb-)
# Get this from: https://api.slack.com/apps -> OAuth & Permissions -> Bot User OAuth Token
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# App-Level Token for Socket Mode (starts with xapp-)
# Get this from: https://api.slack.com/apps -> Socket Mode -> App-Level Tokens
SLACK_APP_TOKEN=xapp-your-app-level-token-here

# Optional: Database Configuration
# For SQLite persistence (default uses in-memory storage)
# DB_PATH=/path/to/decidely.db

# Optional: Logging Level
# Options: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO